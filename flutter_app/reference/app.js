const screens=[...document.querySelectorAll('[data-screen]')];const stages=[...document.querySelectorAll('.stage')];function show(id){screens.forEach(s=>s.classList.toggle('hidden',s.id!==id));stages.forEach(s=>s.classList.toggle('active',s.dataset.stage===id));window.location.hash=id}stages.forEach(s=>s.addEventListener('click',()=>show(s.dataset.stage)));document.querySelectorAll('[data-next]').forEach(b=>b.addEventListener('click',()=>show(b.dataset.next)));const initial=location.hash.slice(1);if(initial&&screens.some(s=>s.id===initial))show(initial);
